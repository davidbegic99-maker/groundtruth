# Normalize line endings: store text as LF in the repo (Render runs on Linux).
* text=auto eol=lf

# Treat vendored/binary assets as binary (never line-ending normalized).
*.png binary
*.jpg binary
*.jpeg binary
*.min.js binary
